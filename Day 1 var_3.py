# Variables - 1. It stores values for later use
#             2. It allows to give a label to a piece of data so that we can refer that data using the chosen variable name
# len() - returns the number of items in an object
# Task : Split each step in the previous exercise into a separate variable.
# One variable called username and one called length.
# Use the variable username in the len calculation.


username = input("What is your name?")
length = len(username)
print(length)
